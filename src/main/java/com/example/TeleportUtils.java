package com.example;

import net.minecraft.particle.ParticleEffect;
import net.minecraft.particle.ParticleTypes;
import net.minecraft.server.network.ServerPlayerEntity;
import net.minecraft.util.math.BlockPos;

public class TeleportUtils {

    public static void teleportPlayer(BlockPos savedPos, ServerPlayerEntity player) {

        //// Define a ParticleEffect using var
        //var particleEffect = ParticleTypes.PORTAL;
//
        //// Teleport the player to the target position
        ////player.teleport(savedPos.getX(), savedPos.getY(), savedPos.getZ());
//
        //// Spawn particles at the target position
        //spawnParticles((playerEntity) player.getEntityWorld(), savedPos, particleEffect);
        //// Spawn particles at the target position
        //spawnParticles(client, targetPos, particleEffect);

    //    //MinecraftClient client = MinecraftClient.getInstance();
    //    if (client.world != null) {
    //        // Teleport the player to the target position
//
    //        // Define a ParticleEffect using var
    //        var particleEffect = ParticleTypes.PORTAL;
//
    //        // Spawn particles at the target position
    //        spawnParticles(client, savedPos, particleEffect);
    //    }
//
    //}
//
    //private static void spawnParticles(MinecraftClient world, BlockPos pos, ParticleEffect particleEffect) {
    //    // Spawn particles at the target position
    //    world.particleManager.addParticle(particleEffect, pos.getX(), pos.getY(), pos.getZ(), 0, 0, 0);
    //}
        return;
    }}
